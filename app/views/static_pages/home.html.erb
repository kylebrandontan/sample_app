<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
  </head>
  <body>
<% if logged_in? %>
  <%= render 'shared/microposts' %>
<% else %>
  <%= render 'shared/welcome' %>
    </body>
</html>

<% end %>
